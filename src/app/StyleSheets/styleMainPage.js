const styleMainPage = () => {
  const { innerHeight } = window;
  return {
    background: {
      minHeight: innerHeight,
      width: '100%',
      backgroundColor: '#0a192f'
    }
  };
};
