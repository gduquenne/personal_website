const developIdea = idea => {
  return String.toString(idea);
};

console.log(developIdea('ScrollUpdate'));
// expected output: "Scroll event remake. Means, scroll will scroll section per section."
